assembly-versioning-scheme: MajorMinorPatch
mode: Mainline
next-version: 1.1.6
continuous-delivery-fallback-tag: ""
branches:
  master:
    regex: master
    tag: 
    increment: none
  stable:
    regex: (stable)
    tag: 
    increment: none
  dev:
    regex: dev/.*?/(.*?)
    tag: dev.{BranchName}
    increment: patch
ignore:
  sha: []